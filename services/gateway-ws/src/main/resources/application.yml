server:
  port: 8080
spring:
  application:
    name: gateway-ws
  profiles:
    active: kafka

management:
  endpoints:
    web:
      exposure:
        include: "*"
  tracing:
    sampling:
      probability: 1.0
  metrics:
    export:
      prometheus:
        enabled: true
